import React from 'react';
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card';

const ReportsBranches: React.FC = () => (
  <Card>
    <CardHeader>
      <CardTitle>Отчёты филиалов (в разработке)</CardTitle>
    </CardHeader>
    <CardContent></CardContent>
  </Card>
);

export default ReportsBranches;
